let a = new List(['one', 2, 'three', 'IV'])
a.render();

let b = new List2(['one', 2, 'three', 'IV'], 'list');
b.render();

let c = new List3(['one', 2, 'three', 'IV'], ['text-center', 'bold', 'text-red']);
c.render();